#!/bin/bash

# clang-tbb.bldconf - configuration file for PARSEC

source ${PARSECDIR}/pkgs/tools/cmake/parsec/clang.bldconf

# Package dependencies
# NOTE: cmake itself does not depend on tbb but the
# library is added globally and hence we must add it
build_deps="tbb"

