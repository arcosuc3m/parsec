#!/bin/bash

source ${PARSECDIR}/pkgs/netapps/netferret/parsec/clang.bldconf

# Environment to use for configure script and Makefile
# NOTE: $netferret_flags is set in clang.bldconf
build_env="version=\"serial\" ${netferret_flags}"

